import { PlaceholderPage } from "@/components/placeholder-page";

export default function ReportesPage() {
  return (
    <PlaceholderPage
      title="Reportes"
      description="Dashboards BI y motor de semáforo (secciones 5 y 7) — próxima porción del sistema a construir."
    />
  );
}
